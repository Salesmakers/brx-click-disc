import ClickupDiscord from "./modules/ClickupDiscord";
import BaseHendler from "./modules/BaseHendler";


document.addEventListener("DOMContentLoaded", () => {
    const baseHanle = new BaseHendler();

    if (brX.base.lenght !== 0) {

        const ClicToDisc = new ClickupDiscord();
        // baseHanle.content.setAttribute("hidden", "hidden");
        // baseHanle.content.removeAttribute("hidden");
    }

});

